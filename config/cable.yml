development:
  adapter: async

test:
  adapter: test

staging:
  adapter: redis
  url: <%= ENV.fetch("REDIS_CABLE_URL", "redis://localhost:6379/1") %>
  channel_prefix: registrar_center_staging

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_CABLE_URL", "redis://localhost:6379/1") %>
  channel_prefix: registrar_center_production
